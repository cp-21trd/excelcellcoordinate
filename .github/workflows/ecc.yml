name: Build + Test

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: build
        shell: pwsh
        run: |
         Invoke-Build .